## 🔹 Обозначения

- $ G = (V, E) $ — граф, $ |V| = n $.
- $ d(u) $ — масса потребителей в вершине $ u \in V $.
- $ m $ — число фирм.
- $ x_i \in \Delta^{n-1} $ — стратегия фирмы $ i $: распределение вероятностей по вершинам $ V $.  
  То есть $ x_i(v) \geq 0 $, $ \sum_{v \in V} x_i(v) = 1 $.
- $ p_i \geq 0 $ — цена фирмы $ i $.
- $ \text{dist}(u,v) $ — кратчайшее расстояние от $ u $ до $ v $ в графе.

---

## 🔹 Стоимость для потребителя

Пусть фирма $ i $ распределена по вершинам с вероятностями $ x_i(v) $. Тогда **ожидаемая стоимость** для потребителя в вершине $ u $ при выборе фирмы $ i $:

$$
C_i(u) = p_i + \sum_{v \in V} x_i(v) \cdot \text{dist}(u,v)
$$

(линейная по $ x_i $).

---

## 🔹 Вероятность выбора фирмы

Пусть потребитель в вершине $ u $ выбирает фирму с минимальной полной стоимостью. Чтобы получить **непрерывную аппроксимацию**, можно использовать **softmin** (например, logit-модель):

$$
q_i(u) = \frac{\exp(-\lambda C_i(u))}{\sum_{j=1}^m \exp(-\lambda C_j(u))}
$$

где $ \lambda > 0 $ — параметр "рациональности" потребителя. При $ \lambda \to \infty $ получаем жёсткий минимум.

---

## 🔹 Ожидаемая прибыль фирмы

Тогда ожидаемая прибыль фирмы $ i $:

$$
\pi_i(x_i, p_i; x_{-i}, p_{-i}) = p_i \cdot \sum_{u \in V} d(u) \cdot q_i(u)
$$

Это **гладкая функция** по $ x_i $ и $ p_i $, если $ \lambda $ фиксировано.

---

## 🔹 Связь с исходной моделью

Если $ x_i $ — **дегеративное распределение** (т.е. $ x_i(v) = 1 $ для некоторой вершины $ v $), и $ \lambda \to \infty $, то:
- $ C_i(u) \to p_i + \text{dist}(u,v) $
- $ q_i(u) \to \mathbf{1}\{i = \arg\min_j C_j(u)\} $
- $ \pi_i \to $ прибыль в исходной дискретной модели.

Таким образом, **эта непрерывная модель обобщает дискретную и совпадает с ней на вершинах**.

---

## 🔹 Задача оптимизации

Для каждой фирмы $ i $:

$$
\max_{x_i \in \Delta^{n-1}, \; p_i \geq 0} \quad \pi_i(x_i, p_i; x_{-i}, p_{-i})
$$

Можно добавить регуляризацию (например, энтропию $ H(x_i) $) или штрафы на цену.

---

## 🔹 Возможные методы решения

- **Convex-concave procedures** (если фиксировать $ p_i $, задача по $ x_i $ — выпуклая).
- **Mirror descent / projected gradient** по $ x_i $ на симплексе.
- **Alternating best response**: поочерёдная оптимизация по $ (x_i, p_i) $ при фиксированных стратегиях конкурентов.
- **Entropic regularization** для ускорения сходимости и сглаживания.


---

## 🔹 Энтропийная регуляризация стратегии

Пусть стратегия фирмы $i$ — распределение $x_i \in \Delta^{n-1}$ по вершинам графа $V$. Тогда **энтропия** стратегии:

$$
H(x_i) = -\sum_{v \in V} x_i(v) \log x_i(v)
$$

Эта функция:
- максимальна при равномерном распределении,
- минимальна при детерминированной стратегии (энтропия = 0).

---

## 🔹 Регуляризованная функция прибыли

Добавим штраф за стохастичность с коэффициентом $\beta > 0$. Тогда **регуляризованная прибыль фирмы $i$**:

$$
\tilde{\pi}_i(x_i, p_i; x_{-i}, p_{-i}) = p_i \cdot \sum_{u \in V} d(u) \cdot q_i(u) - \beta H(x_i)
$$

где:
- $q_i(u)$ — вероятность, что потребитель в вершине $u$ выберет фирму $i$, например:
  $$
  q_i(u) = \frac{\exp(-\lambda C_i(u))}{\sum_{j=1}^m \exp(-\lambda C_j(u))}
  $$
- $C_i(u) = p_i + \sum_{v \in V} x_i(v) \cdot \text{dist}(u,v)$

---

## 🔹 Задача оптимизации

Для каждой фирмы $i$:

$$
\max_{x_i \in \Delta^{n-1}, \; p_i \geq 0} \quad \tilde{\pi}_i(x_i, p_i; x_{-i}, p_{-i})
$$

где:
- $x_i$ — стратегия на симплексе,
- $p_i$ — цена,
- остальные стратегии $x_{-i}, p_{-i}$ фиксированы.

---

## 🔹 Свойства

- При $\beta = 0$: модель совпадает с исходной стохастической моделью.
- При $\beta \to \infty$: стратегия стремится к детерминированной (энтропия → 0).
- При $\lambda \to \infty$: потребители выбирают строго минимальную стоимость → модель приближается к исходной дискретной.

---

## 🔹 Возможные методы решения

- **Projected gradient ascent** по $x_i$ на симплексе + по $p_i$ на $\mathbb{R}_+$.
- **Mirror descent** с KL-дивергенцией — естественно работает с энтропией.
- **Alternating best response dynamics**: поочерёдная оптимизация фирм.
- **JAX / PyTorch**: автоматическое дифференцирование, быстрая реализация.

# RL подход 


Модель Хоттелинга на графах можно переформулировать как задачу **многoагентного обучения с подкреплением (Multi-Agent RL)**. Давай разберёмся, как это сделать эффективно и какие постановки дают алгоритмические преимущества.

---

## 🔹 Базовая RL-постановка

### Агент:
Каждая фирма \(i\) — отдельный агент.

### Пространство действий:
- **Дискретное**: выбор вершины \(v_i \in V\) (местоположение).
- **Непрерывное**: возможно, цена \(p_i \in \mathbb{R}_+\).

### Политика:
- \(\pi_i(a_i \mid s)\), где \(a_i = (v_i, p_i)\) — стратегия фирмы.
- Можно параметризовать как softmax over logits на вершинах + регрессия по цене.

### Состояние:
- Может быть пустым (статическая игра), или включать:
  - расположения конкурентов,
  - распределение потребителей,
  - историю спроса.

### Награда:
- Прибыль фирмы:
  \[
  r_i = p_i \cdot \sum_{u \in V} d(u) \cdot \mathbf{1}\left\{i = \arg\min_j (p_j + \text{dist}(u,v_j))\right\}
  \]

---

## 🔹 RL-алгоритмы

### 1. **Independent Q-learning / Policy Gradient**
- Каждая фирма обучается независимо.
- Подходит при фиксированных конкурентах.
- Проблема: нет гарантии сходимости в многoагентной среде.

### 2. **Multi-Agent Actor-Critic (e.g., MADDPG)**
- Централизованный критик, децентрализованные политики.
- Позволяет учитывать стратегии конкурентов.

### 3. **Population-Based Training / Evolutionary RL**
- Эволюция стратегий в популяции фирм.
- Хорошо работает при дискретных действиях.

---

## 🔹 Альтернативная постановка: Марковская игра

Можно рассматривать модель как **однократную игру**, но также как **повторяющуюся игру**, где фирмы адаптируют стратегии во времени:
- Состояние: текущие позиции и цены.
- Действие: новая стратегия.
- Награда: прибыль.
- Переход: обновление позиций.

Это позволяет использовать **policy iteration**, **meta-RL**, **opponent modeling**.

---

## 🔹 Value-функция и сходимость

Оценкп **Value-функции**:
- Для фирмы \(i\):  
  \[
  V_i(s) = \mathbb{E}_{\pi_i, \pi_{-i}} \left[ \sum_t \gamma^t r_i^{(t)} \mid s_0 = s \right]
  \]
- В статической игре: просто ожидаемая прибыль.

Если Value-функция точно аппроксимирована (например, через GNN или таблично), можно использовать:
- **Best-response dynamics**,
- **Fictitious play**,
- **Gradient ascent in policy space**.

---

## 🔹 Альтернатива: RL как приближение к Nash

Можно использовать RL для поиска **приближённого равновесия Нэша**:
- Каждая фирма обучается максимизировать свою Value-функцию.
- Если ни один агент не может улучшить Value — достигнуто равновесие.

--

- агенты — фирмы;
- действия — выбор вершины (местоположение);
- политика — softmax по вершинам;
- награда — доля потребителей, выбравших фирму;
- обучение — REINFORCE с логитами по вершинам.

---

## Архитектура RL-агента

### Параметры агента $i$:
- $\theta_i \in \mathbb{R}^n$ — логиты по вершинам графа $V$.
- Политика:  
  $$
  \pi_i(v) = \frac{\exp(\theta_i(v))}{\sum_{v'} \exp(\theta_i(v'))}
  $$

### Действие:
- Выбор вершины $v_i \sim \pi_i$.

### Награда:
- Число потребителей, выбравших фирму $i$, при фиксированных стратегиях конкурентов.

### Обновление:
- Policy gradient (REINFORCE):  
  $$
  \theta_i \leftarrow \theta_i + \eta \cdot \nabla_{\theta_i} \log \pi_i(v_i) \cdot r_i
  $$

---